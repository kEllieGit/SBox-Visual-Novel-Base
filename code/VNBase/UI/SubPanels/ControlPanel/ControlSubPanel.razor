@using Sandbox;
@using Sandbox.UI;
@using System;

@namespace VNBase.UI

@inherits Panel

<root>

</root>

@code {
    public bool Open { get; set; }

    public void TogglePanel()
    {
        Open = !Open;
        SetClass("showPanel", Open);
        Sound.FromScreen("ui-click");
    }
}